# 🤖 Physical AI & Humanoid Robotics - Environment Variables
# Copy this file to .env in your backend directory and fill in your actual values

# ──────────────────────────────────────────────────────────────────────────────
# 🌐 BACKEND CONFIGURATION
# ──────────────────────────────────────────────────────────────────────────────

# Application Settings
APP_NAME=Physical AI and Humanoid Robotics Book
APP_VERSION=1.0.0
DEBUG=false
SECRET_KEY=your-secret-key-here-use-a-very-long-random-string-for-production

# Frontend Settings
FRONTEND_URL=http://localhost:3005
BACKEND_CORS_ORIGINS=["http://localhost:3005", "http://127.0.0.1:3005", "http://localhost:3000", "http://127.0.0.1:3000", "http://localhost:8080"]

# ──────────────────────────────────────────────────────────────────────────────
# 🤖 AI & ML SERVICES
# ──────────────────────────────────────────────────────────────────────────────

# OpenAI Configuration (Primary AI service for chat, embeddings, and translation)
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_EMBEDDING_MODEL=text-embedding-ada-002
OPENAI_CHAT_MODEL=gpt-3.5-turbo

# Google Gemini Configuration (Alternative AI service - optional)
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.5-pro-flash

# ──────────────────────────────────────────────────────────────────────────────
# 🗄️ DATABASE & STORAGE
# ──────────────────────────────────────────────────────────────────────────────

# PostgreSQL Database (Neon)
NEON_DATABASE_URL=your-neon-db-url-here
DATABASE_URL=your-database-url-here

# Qdrant Vector Database Configuration (For RAG functionality)
QDRANT_URL=https://your-qdrant-cluster.qdrant.io
QDRANT_API_KEY=your-qdrant-api-key-here
QDRANT_COLLECTION_NAME=book_content

# Content Directory Settings
CONTENT_DIRECTORY=./docusaurus/docs

# ──────────────────────────────────────────────────────────────────────────────
# 🔐 SECURITY SETTINGS
# ──────────────────────────────────────────────────────────────────────────────

# JWT Configuration (if using JWT authentication)
JWT_SECRET_KEY=your-jwt-secret-key-here
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Session Configuration
SESSION_SECRET=your-session-secret-here

# ──────────────────────────────────────────────────────────────────────────────
# 📁 FILE STORAGE & PATHS
# ──────────────────────────────────────────────────────────────────────────────

# Upload Configuration
MAX_UPLOAD_SIZE=10MB
ALLOWED_FILE_TYPES=[".png", ".jpg", ".jpeg", ".pdf", ".txt", ".md", ".mdx"]

# ──────────────────────────────────────────────────────────────────────────────
# 📊 LOGGING & MONITORING
# ──────────────────────────────────────────────────────────────────────────────

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE_PATH=./logs/app.log
LOG_MAX_SIZE=10MB

# ──────────────────────────────────────────────────────────────────────────────
# 🔄 CACHE SETTINGS
# ──────────────────────────────────────────────────────────────────────────────

# Redis Configuration (if using Redis for caching)
REDIS_URL=redis://localhost:6379
CACHE_TTL=3600

# ──────────────────────────────────────────────────────────────────────────────
# 📬 EMAIL & NOTIFICATIONS
# ──────────────────────────────────────────────────────────────────────────────

# SMTP Configuration (for user notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here
EMAIL_FROM=your-email@gmail.com

# ──────────────────────────────────────────────────────────────────────────────
# 🛠️ DEVELOPMENT & DEBUGGING
# ──────────────────────────────────────────────────────────────────────────────

# Development mode (set to true only during development)
DEVELOPMENT_MODE=false

# Test configuration
TEST_DATABASE_URL=sqlite:///./test.db
TESTING=false

# ──────────────────────────────────────────────────────────────────────────────
# 📋 INSTRUCTIONS
# ──────────────────────────────────────────────────────────────────────────────

# 1. Copy this file to `.env` in your backend directory
# 2. Replace all placeholder values with your actual API keys and URLs
# 3. Keep this file secure and never commit it to version control
# 4. Make sure to restart your backend server after updating this file
# 5. For production, use proper secret management systems instead of .env files

# Key variables required for the application to function:
# - OPENAI_API_KEY: Required for chat, translation, and embeddings (primary AI service)
# - GEMINI_API_KEY: Optional alternative AI service
# - QDRANT_URL & QDRANT_API_KEY: Required for vector storage (optional, has fallback)
# - SECRET_KEY: Required for security features
# - FRONTEND_URL: Required for CORS configuration
# - NEON_DATABASE_URL: Required for user data storage
# - CONTENT_DIRECTORY: Required for loading book content

# 🚨 SECURITY WARNING: Never commit this file to git or share it publicly!
# Use .gitignore to exclude .env files from version control